/*global describe, it */
'use strict';
var assert = require('assert');
//var metalsmithExternalWatch = require('../');

describe('metalsmith-external-watch node module', function () {
    it('must have at least one test', function () {
        //metalsmithExternalWatch();
        assert(true, 'I was too lazy to write any tests. Shame on me.');
    });
});
